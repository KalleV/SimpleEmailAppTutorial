'use strict';

describe('emailAppControllers', function() {
    beforeEach(module('emailApp'));

    describe('InboxCtrl', function() {
        var scope, ctrl, $httpBackend;

        //beforeEach(inject(function(_$httpBackend_, $rootScope, $controller) {
        //    $httpBackend = _$httpBackend_;
        //    $httpBackend.expectGET('json/emails.json').
        //        respond([{author: 'Bob Smith'}, {message: 'Hello world!'}]);
        //    scope = $rootScope.$new();
        //    ctrl = $controller('InboxCtrl', {$scope: scope});
        //}));
    })
});